'use strict';

/*
//Access tokens service used to communicate Access tokens REST endpoints
angular.module('access-tokens').factory('AccessTokens', ['$resource',
	function($resource) {
		return $resource('access-tokens/:accessTokenId', { accessTokenId: '@_id'
		}, {
			update: {
				method: 'PUT'
			}
		});
	}
]);*/
